#include <iostream>
#include <string>

int main()
{
	std::string alp = "0123456789";
	n = alp.length() - 1;
	int pos = 0 , len = 5;
	std::string word;
	int aux[len];
	for (;;)
	{
		if (pos >= len)
			break;

		for (i = 0; i < len; i++)
			aux[i] = 0;

		for (int i = 0; i < n; ++i)
		{
			
		}
	}
}